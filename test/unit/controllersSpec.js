'use strict';

/* jasmine specs for controllers go here */
describe('justPlay controllers', function() {
/*     describe('friendModuleCtrl', function(){
      var scope, ctrl;
      beforeEach(angular.mock.module('friendModule'));
      beforeEach(angular.mock.inject(function($controller) {
        scope = {};
        ctrl = $controller('test', {$scope:scope});
      }));
      it('should have 3 items', inject(function($controller) {
        expect(scope.dataz.length).toBe(3);
      }));
    });*/

});